import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  link: {
    paddingLeft: 24,
  },
  dropDown: {
    width: 400,
    margin: 'auto',
  },
}));

export default useStyles;
